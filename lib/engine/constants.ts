export const DEFAULT_DEEP_WORK_MIN_DURATION = 60; // minutes
export const DEFAULT_BREAK_INTERVAL = 60; // minutes
export const DEFAULT_BREAK_DURATION = 10; // minutes
export const LATE_START_TOLERANCE = 5; // minutes, start adjusting if more than this
export const MIN_SESSION_DURATION = 15; // minutes, never compress below this unless deep work
